apiVersion: apps/v1
kind: Deployment
metadata:
  name: autowebiq-redis
  namespace: autowebiq
spec:
  replicas: 1
  selector:
    matchLabels:
      app: autowebiq-redis
  template:
    metadata:
      labels:
        app: autowebiq-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: autowebiq-redis
  namespace: autowebiq
spec:
  selector:
    app: autowebiq-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
  type: ClusterIP